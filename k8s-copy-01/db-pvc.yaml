apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: prisma
  name: db-claim0
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard